#ifndef SBASH64_BUDGET_TEST_COLLECT_HPP_
#define SBASH64_BUDGET_TEST_COLLECT_HPP_

#include <sbash64/testcpplite/testcpplite.hpp>

namespace sbash64::budget::collect {
void expensesToTree(testcpplite::TestResult &);
} // namespace sbash64::budget::collect

#endif
